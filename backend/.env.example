# Server
PORT=8081
HOST=0.0.0.0

# Auth / DB
JWT_SECRET=
TOKEN_TTL_HOURS=168
DB_PATH=./nicefilm.db

# Subtitles — SubDL (https://subdl.com/panel/api) is the only provider wired in.
# Optional shared fallback key; users can supply their own in Profile →
# Connections. Search/download is disabled without either.
SUBDL_API_KEY=
