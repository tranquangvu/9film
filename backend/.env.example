# Server
PORT=8081
HOST=0.0.0.0

# Auth / DB
JWT_SECRET=
TOKEN_TTL_HOURS=168
DB_PATH=./nicefilm.db

# OpenSubtitles (optional — subtitle search/download disabled if unset)
OPENSUBTITLES_API_KEY=
OPENSUBTITLES_USERNAME=
OPENSUBTITLES_PASSWORD=

# Gemini — configured per-user only (each user supplies their own key in
# settings). The server reads no GEMINI_API_KEY from the environment.
