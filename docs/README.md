# 9Film docs

Short pages, one job each. The root [README](../README.md) is for someone who just wants to install and run it.

| | |
|---|---|
| [Getting started](getting-started.md) | Prerequisites, the dev loop, every command |
| [Architecture](architecture.md) | What talks to what, and how the code is laid out |
| [API](api.md) | Every route the backend serves |

`CLAUDE.md` at the repo root is the same territory as *Architecture*, but written for whoever is editing the code — every constraint, and why it exists.
